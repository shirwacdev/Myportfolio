const SectionNotice = ({ message }) => (
  <p className="font-code mt-4 rounded-2xl border border-[var(--border-soft)] bg-[var(--surface-muted)] px-4 py-3 text-xs uppercase tracking-[0.12em] text-[var(--text-muted)]">
    {message}
  </p>
)

export default SectionNotice
